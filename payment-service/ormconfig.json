{ 
   "type": "postgres", 
   "host": "localhost", 
   "port": 5432, 
   "username": "postgres",  
   "password": "12032001", 
   "database": "mydb",  
   "synchronize": true, 
   "logging": false, 
   "entities": [
      "src/entities/**/*.ts}"
   ],
   "migrations": [
      "src/migrations/**/*.ts}"
   ],
   "subscribers": [ "src/subscriber/**/*.{js,ts}" 
   ], 
   "cli": { 
      "entityDir":"src/entities", "migrationsDir":"src/migrations", "subscribersDir":"src/subscriber"
   }    
}